#Python program to sum of three given integers. However, if two values are
#equal sum will be zero


def sum(x,y,z):
    if x==y or x==z or y==z or x==y:
        total=0
        return total
    else:
        return x+y+z
print(sum(2,4,3))
